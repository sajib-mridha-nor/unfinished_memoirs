import 'package:flutter/material.dart';

const topBorderRadius = BorderRadius.only(
  topLeft: Radius.circular(30),
  topRight: Radius.circular(30),
);

const circularBorderRadius10 = BorderRadius.all(Radius.circular(10));
const circularBorderRadius15 = BorderRadius.all(Radius.circular(15));
const circularBorderRadius20 = BorderRadius.all(Radius.circular(20));
const circularBorderRadius25 = BorderRadius.all(Radius.circular(25));
const circularBorderRadius30 = BorderRadius.all(Radius.circular(30));
const circularBorderRadius50 = BorderRadius.all(Radius.circular(50));
