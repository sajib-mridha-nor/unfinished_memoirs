import 'package:flutter/material.dart';

const leftRightPadding = EdgeInsets.only(left: 15, right: 15);
const leftRightBottomPadding = EdgeInsets.only(left: 20, right: 20, bottom: 10);
const zeroPadding = EdgeInsets.zero;
const allPadding20 = EdgeInsets.all(20);
const allPadding25 = EdgeInsets.all(25);
const commonScaffoldPadding = EdgeInsets.only(left: 20, right: 20, top: 20);
const modalSheetPadding =
    EdgeInsets.only(left: 25, right: 25, top: 15, bottom: 15);
const allPadding15 = EdgeInsets.all(15);
const surahSelectPadding =
    EdgeInsets.only(left: 15, right: 15, top: 20, bottom: 30);
const leftPadding15 = EdgeInsets.only(left: 15);
const leftRightPadding15 = EdgeInsets.only(left: 15, right: 15);
const allPadding10 = EdgeInsets.all(10);
const allPadding12 = EdgeInsets.all(12);
